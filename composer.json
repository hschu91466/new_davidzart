{
  "require": {
    "vlucas/phpdotenv": "^5.6",
    "aws/aws-sdk-php": "^3.384"
  },
  "scripts": {
    "serve": "php -S localhost:8000 -t public",
    "serve:router": "php -S localhost:8000 -t public public/index.php"
  }
}
